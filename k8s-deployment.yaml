apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-simulasi-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: devops-simulasi-app
  template:
    metadata:
      labels:
        app: devops-simulasi-app
    spec:
      containers:
      - name: devops-simulasi-app
        image: devops-simulasi-app
        ports:
        - containerPort: 5000